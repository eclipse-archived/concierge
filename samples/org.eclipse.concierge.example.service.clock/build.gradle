publishing {
    publications {
   	    mavenJava(MavenPublication) {
	   	    groupId 'org.eclipse.concierge'
            artifactId 'org.eclipse.concierge.example.service.clock'
            version version
			pom {
				name = 'Eclipse Concierge'
				description = 'Example Service Clock'
			}
       	}
   	}
}
