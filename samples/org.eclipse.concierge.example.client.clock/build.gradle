publishing {
    publications {
   	    mavenJava(MavenPublication) {
	   	    groupId 'org.eclipse.concierge'
            artifactId 'org.eclipse.concierge.example.client.clock'
            version version
			pom {
				name = 'Eclipse Concierge'
				description = 'Example Client Clock'
			}
       	}
   	}
}
