publishing {
    publications {
   	    mavenJava(MavenPublication) {
	   	    groupId 'org.eclipse.concierge'
            artifactId 'org.eclipse.concierge.service.startlevel'
            version version
			pom {
				name = 'Eclipse Concierge'
				description = 'StartLevel Service'
			}
       	}
   	}
}
