publishing {
    publications {
   	    mavenJava(MavenPublication) {
	   	    groupId 'org.eclipse.concierge'
            artifactId 'org.eclipse.concierge.service.permission'
            version version
			pom {
				name = 'Eclipse Concierge'
				description = 'Service with Permission classes'
			}
       	}
   	}
}