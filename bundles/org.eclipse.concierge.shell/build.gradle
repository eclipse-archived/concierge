publishing {
    publications {
   	    mavenJava(MavenPublication) {
	   	    groupId 'org.eclipse.concierge'
            artifactId 'org.eclipse.concierge.shell'
            version version
			pom {
				name = 'Eclipse Concierge'
				description = 'Shell - Simple OSGi Console'
			}
       	}
   	}
}